apiVersion: v1
kind: Service
metadata:
  name: {{ include "clienteling-backend.fullname" . }}
  labels:
    {{- include "clienteling-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clienteling-backend.selectorLabels" . | nindent 4 }}
